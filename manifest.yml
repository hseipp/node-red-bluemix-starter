applications:
  - path: .
    name: letwatsonguess
    environment_json: {}
    memory: 256M
    env:
      OPTIMIZE_MEMORY: true
    instances: 1
    disk_quota: 1024M
    command: node index.js --settings ./bluemix-settings.js -v
